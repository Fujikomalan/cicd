
name: CI
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: "Checkout to branch"
        uses: actions/checkout@v3
        
      - name: "Listing Directory Contents"
        run: ls -al /boot/
        
        

